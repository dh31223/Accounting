[project]
name = "accounting"
version = "0.1.0"
description = "个人记账软件 — 支持收支记录、统计分析、预算管理、账单提醒"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "PyQt6>=6.5",
    "matplotlib>=3.7",
    "openpyxl>=3.1",
    "httpx>=0.25",
]
